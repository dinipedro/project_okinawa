/**
 * Re-export from Identity module (canonical source)
 * Auth module consumers should use this for backward compatibility.
 * The canonical entity lives in @/modules/identity/entities/user-credential.entity
 */
export { UserCredential } from '@/modules/identity/entities/user-credential.entity';
